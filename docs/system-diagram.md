# Athena Query Tool — System Flow

```
  ┌──────────────────────────────────────┐
  │             👤  USER                 │
  │       "investigate X"                │
  └──────────────────┬───────────────────┘
                     │ triage question
                     ▼
  ┌──────────────────────────────────────┐
  │            🧠  CLAUDE                │
  │   plans broad query strategy         │
  │   edits query.py INPUT section       │
  └──────────────────┬───────────────────┘
                     │ Edit tool
                     ▼
  ╔══════════════════════════════════════╗
  ║        🔒  PRE-TOOL HOOK             ║
  ║        protect-validate-fn.sh        ║
  ║        guards validate_read_only()   ║
  ║        blocks any edit to it         ║
  ╚══════════════════╤═══════════════════╝
                     │ ✓ approved
                     ▼
  ┌──────────────────────────────────────┐   shows SQL
  │            🧠  CLAUDE                │ ────────────▶  👤 USER
  │   shows query · waits for OK        │ ◀──────────  confirms ✓
  └──────────────────┬───────────────────┘
                     │ confirmed ✓
                     ▼
  ┌──────────────────────────────────────┐
  │            🧠  CLAUDE                │
  │   poetry run python query.py         │
  └──────────────────┬───────────────────┘
                     │ Bash tool
                     ▼
  ╔══════════════════════════════════════╗
  ║        📅  PRE-TOOL HOOK             ║
  ║        check-query-dt.sh             ║
  ║        rejects queries missing       ║
  ║        a dt partition filter         ║
  ╚══════════════════╤═══════════════════╝
                     │ ✓ approved
                     ▼
  ┌──────────────────────────────────────┐
  │           ⚙️   query.py              │
  │                                      │
  │   load_query()                       │
  │   substitute_params()                │
  │   validate_read_only()               │
  │     └─ 🚫 raises on writes          │
  │                  │                   │
  │                  ▼                   │
  │   StartQueryExecution ───────────────┼──┐
  └──────────────────────────────────────┘  │ API call
                                            ▼
  ┌──────────────────────────────────────────────────────┐
  │                  ☁️   AWS ATHENA                      │
  │                                                      │
  │   execute SQL against S3-backed telemetry tables     │
  │    ├── accounts      ├── sessions                    │
  │    ├── users         ├── api_requests                │
  │    └── error_logs                                    │
  │                                                      │
  │   scan dt partitions only  ·  poll until SUCCEEDED   │
  │   GetQueryResults (paginated, 1000 rows/page)        │
  └──────────────────────────┬───────────────────────────┘
                             │ results
                             ▼
  ┌──────────────────────────────────────┐
  │           ⚙️   query.py              │
  │                                      │
  │   format_results()                   │
  │   save → output/query_TIMESTAMP.txt  │
  └──────────────────┬───────────────────┘
                     │ Bash complete
                     ▼
  ╔══════════════════════════════════════╗
  ║        🧹  POST-TOOL HOOK            ║
  ║        cleanup-output.sh             ║
  ║        count files in output/        ║
  ║        delete oldest if > 25         ║
  ╚══════════════════╤═══════════════════╝
                     │ results text
                     ▼
  ┌──────────────────────────────────────┐
  │            🧠  CLAUDE                │
  │   reads & explains results           │
  │   spots anomalies · hypothesizes     │
  │   decides: dig deeper or wrap up     │
  └──────────┬───────────────────────────┘
             │
     ┌───────┴────────┐
     │  root cause?   │
     └───────┬────────┘
             │
   no ◀──────┴──────▶ yes
   │                  │
   ▼                  ▼
(loop — plan    ┌──────────────────────────────────────┐
 next query)    │            👤  USER                  │
                │  📋 findings · root cause · evidence │
                └──────────────────────────────────────┘
```